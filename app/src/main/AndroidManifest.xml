<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fat2fit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fat2Fit">
        <activity android:name=".SignupActivity" />
        <activity android:name=".CustomerRedEditChallengeActivity" />
        <activity android:name=".CustomerRepAddChallengeActivity" />
        <activity android:name=".CustomerRepChallengesActivity" />
        <activity android:name=".Join_LeaveGroupActivity" />
        <activity android:name=".WorkoutDetailsActivity" />
        <activity android:name=".api.ApiDebugActivity" />
        <activity android:name=".ForgotPasswordTwoActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RecommendedWorkoutActivity" />
        <activity android:name=".GroupCreationActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>